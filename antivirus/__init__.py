"""Entry point for `python -m antivirus`.

Most code lives in `<repo>/src/`. This package is a thin shim:
__main__.py adds src/ to sys.path and runs the tray + dashboard launcher.
"""
__version__ = "0.6.0"
